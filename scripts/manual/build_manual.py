# -*- coding: utf-8 -*-
"""Собирает инструкцию по QP VPN в один HTML-файл, готовый к печати в PDF."""

import base64
import io
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).parent))
import segno
from gen_css import CSS
import mockups as M

ROOT = pathlib.Path(__file__).resolve().parents[2]
BASE = "https://github.com/viktorbeloglazov-design/vpn/releases/download/latest"

LINKS = [
    ("Android", "8.0 и новее", f"{BASE}/QPVPN-android.apk"),
    ("Mac", "macOS 13 и новее", f"{BASE}/QPVPN-mac.dmg"),
    ("Windows", "10 и 11", f"{BASE}/QPVPN-windows.zip"),
]

TOTAL = 10


def qr(text):
    """QR-код в виде встроенного SVG — печатается без обращения к сети."""
    buf = io.BytesIO()
    segno.make(text, error="m").save(buf, kind="svg", omitsize=True, border=2,
                                     dark="#0A1015", svgclass=None, xmldecl=False)
    return buf.getvalue().decode("utf-8")


def logo():
    data = (ROOT / "windows/app/Assets/logo-dark.png").read_bytes()
    return "data:image/png;base64," + base64.b64encode(data).decode()


def page(num, title, body, kicker="QP VPN · инструкция"):
    return f"""
<section class="page">
  <div class="head"><span class="who">{kicker}</span><span class="num">{num} / {TOTAL}</span></div>
  {body}
  <div class="foot"><span>{title}</span><span>Ссылки на сборки не меняются — стр. 1</span></div>
</section>"""


# ─────────────────────────── стр. 1 — обложка ───────────────────────────

def cover():
    items = "".join(f"""
      <div class="item">
        <div class="os">{os}</div><div class="ver">{ver}</div>
        <div class="qr">{qr(url)}</div>
        <div class="url">{url.replace(BASE + '/', '')}</div>
      </div>""" for os, ver, url in LINKS)
    return f"""
<section class="page cover">
  <img class="logo" src="{logo()}" alt="Kupibas" />
  <h1>QP VPN</h1>
  <div class="lead">Premium VPN · Special for Kupibas Group. Выход в Казахстане.
    Одна кнопка «включить» — и заблокированное открывается, а МАХ, госуслуги,
    банки и маркетплейсы продолжают видеть российский адрес.</div>

  <div class="dl">{items}</div>

  <div class="perm">
    <b>Эти ссылки постоянные.</b> Они не зависят от номера версии — по ним всегда
    лежит свежая сборка. Сохраните эту страницу или наведите камеру на QR-код.
    Все ссылки начинаются одинаково:<br>
    <span style="font-family:'Liberation Mono',monospace;font-size:8pt">{BASE}/</span>
  </div>

  <div class="toc">
    <div class="toc-h">Что внутри — по листу на каждое устройство</div>
    <div class="toc-grid">
      <div><b>2</b> Android — установка</div>
      <div><b>3</b> Android — куда загрузить ключ</div>
      <div><b>4</b> Mac — установка</div>
      <div><b>5</b> Mac — куда загрузить ключ</div>
      <div><b>6</b> Windows — установка</div>
      <div><b>7</b> Windows — куда загрузить ключ</div>
      <div><b>8</b> iPhone — пока в обход</div>
      <div><b>9</b> Что можно настроить под себя</div>
      <div><b>10</b> Если что-то не работает</div>
    </div>
  </div>

  <div class="foots">
    Экраны в инструкции нарисованы по вёрстке самих приложений: подписи,
    цвета и порядок карточек — те же, что вы увидите на устройстве.
  </div>
</section>"""


# ───────────────────────── стр. 2–3 — Android ─────────────────────────

def android_install():
    body = f"""
  <h1>Android — установка</h1>
  <p class="sub">5 минут. Приложение ставится файлом, мимо Google Play.</p>
  <div class="split">
    <div class="left">{M.phone_home()}
      <div class="cap">Так выглядит главный экран,<br>когда всё работает</div>
    </div>
    <div class="right">
      <ol class="steps">
        <li>Наведите камеру телефона на <b>QR-код Android</b> с первой страницы —
          или откройте ссылку в браузере телефона.</li>
        <li>Браузер спросит, скачивать ли файл. Скачайте — это
          <span class="mono">QPVPN-android.apk</span>.</li>
        <li>Откройте скачанный файл: шторка сверху или «Загрузки» в браузере.</li>
        <li>Android скажет, что установка из этого источника запрещена. Нажмите
          <b>«Настройки»</b> &#8594; разрешите установку для браузера &#8594;
          кнопка «Назад» &#8594; <b>«Установить»</b>.<br>
          <span style="color:#5B6C78;font-size:9pt">Это разовое разрешение —
          оно нужно, потому что приложение не из магазина.</span></li>
        <li>Откройте приложение. Загрузите ключ — как, на следующей странице.</li>
        <li>Нажмите круглую кнопку. Android один раз спросит разрешение на VPN —
          нажмите <b>«ОК»</b>.</li>
        <li>Разрешите уведомления, когда приложение попросит.</li>
      </ol>
      <div class="note"><b>Зачем уведомления.</b> Без них Android не покажет значок
        VPN в строке состояния рядом с Wi-Fi и 4G — вы не будете видеть,
        включена защита или нет. Само уведомление можно свернуть, оно не шумит.</div>
    </div>
  </div>"""
    return page(2, "Android — установка", body)


def android_config():
    body = f"""
  <h1>Android — куда загрузить ключ</h1>
  <p class="sub">Вкладка «Профиль» внизу экрана. Подойдёт любой из четырёх способов.</p>
  <div class="split">
    <div class="left">{M.phone_profile()}
      <div class="cap">Вкладка «Профиль»</div>
    </div>
    <div class="right">
      <table>
        <tr><th style="width:42mm">Что у вас есть</th><th>Что нажать</th></tr>
        <tr><td><b>QR-код на другом экране</b><br>(ноутбук, второй телефон)</td>
            <td>«Сканировать QR из Amnezia» — наведите камеру</td></tr>
        <tr><td><b>QR-код картинкой</b><br>(прислали в чат, сняли экран)</td>
            <td>«QR со снимка экрана» — выберите картинку из галереи</td></tr>
        <tr><td><b>Ссылка vpn://…</b></td>
            <td>Скопируйте ссылку, нажмите «Вставить ссылку vpn://» и вставьте</td></tr>
        <tr><td><b>Файл .conf</b></td>
            <td>«Выбрать файл .conf» — найдите файл в загрузках</td></tr>
      </table>
      <div class="note"><b>Где взять ключ.</b> Ключ выдаётся с вашего сервера —
        один ключ на одно устройство. Если ключ вам присылает администратор,
        попросите QR-код: его проще всего перенести на телефон.</div>
      <div class="warn"><b>Ключ — это пароль от VPN.</b> Ссылка
        <span class="mono">vpn://…</span> и файл <span class="mono">.conf</span>
        содержат приватный ключ. Кто их получит — сможет пользоваться вашим
        сервером от вашего имени. Не пересылайте их в общих чатах и не
        выкладывайте на диски с открытым доступом.</div>
      <div class="note" style="background:#F2F6F9;border-color:#94A4B0">
        Загрузился — на карточке вместо «Профиль не загружен» появится
        <b>«Профиль загружен»</b> и адрес сервера. Можно возвращаться на «Главную»
        и включать.</div>
    </div>
  </div>"""
    return page(3, "Android — ключ", body)


# ─────────────────────────── стр. 4–5 — Mac ───────────────────────────

def mac_install():
    body = f"""
  <h1>Mac — установка</h1>
  <p class="sub">Приложение и служба. Служба нужна обязательно — без неё
    переключатель не сработает.</p>
  <div class="split">
    <div class="wide">{M.mac_home()}
      <div class="cap">Главный экран. Значок также появляется в строке меню сверху</div>
      <div class="note" style="margin-top:4mm"><b>Можно одной командой.</b>
        Откройте «Терминал» и вставьте строку ниже — она скачает свежую сборку,
        положит в «Программы» и откроет. Так надёжнее: файл, скачанный браузером,
        macOS помечает как «из интернета» и потом может вообще отказаться открывать.
        <div class="cmd">curl -fsSL https://raw.githubusercontent.com/viktorbeloglazov-design/vpn/claude/mac-vpn-app-routes-7jv8o7/scripts/install-mac.sh | bash</div>
      </div>
    </div>
    <div class="right">
      <ol class="steps">
        <li>Скачайте <span class="mono">QPVPN-mac.dmg</span> по ссылке с первой
          страницы.</li>
        <li>Откройте образ и перетащите <b>QPVPN</b> в «Программы».</li>
        <li>Первый запуск macOS заблокирует. Откройте
          <b>Системные настройки &#8594; Конфиденциальность и безопасность</b>,
          пролистайте вниз и нажмите <b>«Открыть всё равно»</b>.</li>
        <li>Нажмите <b>«Установить службу»</b> и введите пароль администратора.
          Служба поднимает туннель и правит маршруты — приложение само этого
          не умеет.</li>
        <li>Вкладка <b>«Сервер»</b> — загрузите ключ (следующая страница).</li>
        <li>Вернитесь на «Главную» и нажмите круглую кнопку.</li>
      </ol>
      <div class="note"><b>Почему macOS ругается.</b> Приложение подписано своим
        сертификатом, а не купленным у Apple за 99 $ в год. Разрешить нужно
        один раз.</div>
      <div class="warn"><b>Если значок прыгает в Dock и гаснет</b> — macOS отказала
        молча. Выполните в «Терминале»:
        <div class="cmd">xattr -dr com.apple.quarantine /Applications/QPVPN.app</div>
      </div>
    </div>
  </div>"""
    return page(4, "Mac — установка", body)


def mac_config():
    body = f"""
  <h1>Mac — куда загрузить ключ</h1>
  <p class="sub">Вкладка «Сервер». Подойдёт любой из четырёх способов.</p>
  <div class="split">
    <div class="mid">{M.mac_server()}
      <div class="cap">Вкладка «Сервер»</div>
      <div class="note" style="margin-top:4mm">
        <b>Проверьте, что получилось.</b> Включите VPN и нажмите на «Главной»
        <b>«Проверить мой IP»</b> — должно появиться
        <b>KZ</b> и казахстанский адрес. Если видите российский — ключ загружен,
        но туннель не поднялся: смотрите последнюю страницу.</div>
    </div>
    <div class="right">
      <table>
        <tr><th style="width:38mm">Что у вас есть</th><th>Что сделать</th></tr>
        <tr><td><b>Файл .conf</b> или <b>картинка с QR</b></td>
            <td>«Открыть файл или QR-код…» — или просто перетащите файл
                на окно приложения</td></tr>
        <tr><td><b>Ссылка vpn://…</b> в буфере</td>
            <td>«Вставить из буфера»</td></tr>
        <tr><td><b>QR-код на экране</b></td>
            <td>Снимите экран (&#8984;&#8679;4), затем «Открыть файл или QR-код…»
                и выберите снимок</td></tr>
        <tr><td><b>Текст конфига</b></td>
            <td>«Вставить текстом…» и вставьте целиком</td></tr>
      </table>
      <div class="note">Загрузилось — появится строка
        <b>«Конфигурация заполнена»</b>. Ниже можно посмотреть и поля по отдельности:
        адрес сервера, ключи, адрес в туннеле. Менять их вручную не нужно.</div>
      <div class="warn"><b>Ключ — это пароль от VPN.</b> Приватный ключ хранится
        в защищённой папке, куда обычные программы не заглядывают. Но сама ссылка
        <span class="mono">vpn://…</span>, пока она у вас в переписке, —
        готовый доступ к серверу. Удалите её из чата после загрузки.</div>
    </div>
  </div>"""
    return page(5, "Mac — ключ", body)


# ───────────────────────── стр. 6–7 — Windows ─────────────────────────

def win_install():
    body = f"""
  <h1>Windows — установка</h1>
  <p class="sub">Программа не ставится в систему — она просто лежит папкой
    и запускается.</p>
  <div class="split">
    <div class="wide">{M.win_home()}
      <div class="cap">Главное окно</div>
    </div>
    <div class="right">
      <ol class="steps">
        <li>Скачайте <span class="mono">QPVPN-windows.zip</span> по ссылке
          с первой страницы.</li>
        <li><b>Распакуйте архив в постоянную папку</b> — например
          <span class="mono">C:\\QPVPN</span>. Не запускайте программу прямо
          из архива и не оставляйте её в «Загрузках».</li>
        <li>Windows SmartScreen скажет, что издатель неизвестен. Нажмите
          <b>«Подробнее»</b> &#8594; <b>«Выполнить в любом случае»</b>.</li>
        <li>Запустите <span class="mono">QPVPN.exe</span>
          <b>от имени администратора</b> — правая кнопка мыши по файлу.</li>
        <li>Загрузите ключ (следующая страница) и нажмите круглую кнопку.</li>
      </ol>
      <div class="warn"><b>Не разделяйте файлы в папке.</b> Рядом с
        <span class="mono">QPVPN.exe</span> лежат
        <span class="mono">qpvpn-tunnel.exe</span> и
        <span class="mono">wintun.dll</span> — это служба туннеля и сетевой адаптер.
        Без них кнопка не сработает. Ярлык на рабочий стол делайте
        <i>ярлыком</i>, а не копией файла.</div>
      <div class="note"><b>Почему нужен администратор.</b> Чтобы поднять туннель
        и проложить маршруты, нужно создать сетевой адаптер — обычным правам
        это недоступно. Чтобы не нажимать «от имени администратора» каждый раз:
        правая кнопка по ярлыку &#8594; «Свойства» &#8594; «Дополнительно»
        &#8594; галочка «Запуск от имени администратора».</div>
    </div>
  </div>"""
    return page(6, "Windows — установка", body)


def win_config():
    body = f"""
  <h1>Windows — куда загрузить ключ</h1>
  <p class="sub">Карточка «Ключ» в главном окне, ниже переключателя.</p>
  <div class="split">
    <div class="mid">{M.win_profile()}
      <div class="cap">Карточка «Ключ» и «Запасной вход»</div>
      <div class="note" style="margin-top:4mm"><b>Проверьте, что получилось.</b>
        Включите VPN и нажмите <b>«Проверить мой IP»</b> в карточке «Проверка» —
        должен появиться казахстанский адрес.</div>
    </div>
    <div class="right">
      <table>
        <tr><th style="width:38mm">Что у вас есть</th><th>Что сделать</th></tr>
        <tr><td><b>Ссылка vpn://…</b></td>
            <td>Скопируйте её и нажмите «Вставить ключ vpn:// из буфера»</td></tr>
        <tr><td><b>Файл .conf</b></td>
            <td>«Открыть файл .conf или картинку с QR-кодом»</td></tr>
        <tr><td><b>QR-код картинкой</b></td>
            <td>Той же кнопкой — программа сама прочитает QR с картинки</td></tr>
        <tr><td><b>Файл на рабочем столе</b></td>
            <td>Просто перетащите его на окно программы</td></tr>
      </table>
      <div class="note">Загрузился — вместо «Ключа нет» появится протокол
        и адрес сервера, например <span class="mono">AmneziaWG · 91.201.xx.xx</span>.
        Ключ хранится в <span class="mono">C:\\ProgramData\\QPVPN</span>, куда
        имеет доступ служба туннеля, а обычные программы — нет. При обновлении
        программы он остаётся на месте.</div>
      <div class="warn"><b>Ключ — это пароль от VPN.</b> Не пересылайте ссылку
        <span class="mono">vpn://…</span> в общих чатах.</div>
    </div>
  </div>"""
    return page(7, "Windows — ключ", body)


# ─────────────────────────── стр. 8 — iPhone ───────────────────────────

def iphone():
    body = """
  <h1>iPhone — пока в обход</h1>
  <p class="sub">Честно: своего приложения для iPhone сейчас нет.</p>

  <div class="warn" style="margin-top:0"><b>Почему.</b> Приложение для iPhone
    написано и лежит в проекте, но поставить его на телефон нельзя без платного
    Apple Developer Program — 99 $ в год плюс проверка Apple. Ни через ссылку,
    ни файлом, как на Android, iPhone ставить не умеет: Apple этого не разрешает.
    Как только аккаунт будет — сборка появится на первой странице рядом
    с остальными.</p></div>

  <h2 style="margin-top:8mm">Что работает уже сейчас</h2>
  <p class="sub" style="margin-bottom:4mm">Тот же самый ключ подходит к бесплатному
    приложению <b>AmneziaVPN</b> из App Store — сервер и ключ у вас общие.</p>

  <div class="split">
    <div class="right">
      <ol class="steps">
        <li>App Store &#8594; найдите <b>AmneziaVPN</b> &#8594; установите.</li>
        <li>«Добавить сервер» &#8594; вставьте ссылку
          <span class="mono">vpn://…</span> или отсканируйте QR-код —
          тот же самый, что и для остальных устройств.</li>
        <li>Разрешите добавить конфигурацию VPN, когда iPhone спросит.</li>
        <li>Включите.</li>
      </ol>
    </div>
    <div class="right">
      <table>
        <tr><th>Чем отличается от нашего приложения</th></tr>
        <tr><td><b>Весь трафик пойдёт через VPN.</b> Российские сайты — МАХ,
          госуслуги, банки, маркетплейсы — увидят казахстанский адрес.
          МАХ и банковские приложения при этом могут отказаться работать.</td></tr>
        <tr><td><b>Нет исключений по приложениям.</b> Пропустить МАХ и банки
          мимо туннеля не получится.</td></tr>
        <tr><td><b>Нет автоподбора размера пакета</b> — в гостевых сетях видео
          может подтормаживать.</td></tr>
        <tr><td><b>Нет запасного входа</b> и автообновления.</td></tr>
      </table>
      <div class="note"><b>Как жить с этим.</b> Держите AmneziaVPN выключенным
        по умолчанию и включайте, когда нужно открыть заблокированное.
        Для банков и госуслуг — выключайте. На Android, Mac и Windows этого
        делать не нужно: там разделение зашито в приложение.</div>
    </div>
  </div>"""
    return page(8, "iPhone", body)


# ────────────────────── стр. 9 — обзор возможностей ──────────────────────

def features():
    body = """
  <h1>Что можно настроить под себя</h1>
  <p class="sub" style="margin-bottom:4.5mm">Настроек намеренно мало: маршруты
    зашиты в приложение. Вот всё, что имеет смысл трогать.</p>

  <table>
    <tr><th style="width:44mm">Что это</th><th style="width:26mm">Где</th><th>Зачем трогать</th></tr>

    <tr><td><b>Рабочие ресурсы</b><br><span style="color:#5B6C78">единственный
        переключатель</span></td>
      <td>Главный экран</td>
      <td>Пускает через VPN несколько рабочих адресов, заложенных в приложение.
        Включайте, если нужны именно они. На остальное не влияет.</td></tr>

    <tr><td><b>Размер пакета (MTU)</b></td>
      <td>Android: «Ещё»<br>Mac: «Ещё»<br>Windows: «Скорость»</td>
      <td>Оставьте <b>«Авто»</b>: приложение само проверяет, проходят ли через
        туннель крупные порции данных, и при необходимости уменьшает пакет.</td></tr>

    <tr><td><b>Запасной вход</b></td>
      <td>Android: «Ещё»<br>Mac: «Ещё»<br>Windows: главное окно</td>
      <td>Адрес вида <span class="mono">95.213.0.1:31984</span>. Если до сервера
        не достучаться напрямую, приложение само пойдёт через этот узел. Ключ менять
        не нужно. Пустое поле — не используется.</td></tr>

    <tr><td><b>Замер скорости</b></td>
      <td>Везде</td>
      <td>Качает и отдаёт один и тот же файл через VPN и мимо него. Четыре числа,
        по которым видно, тормозит канал или сам VPN.</td></tr>

    <tr><td><b>Проверить мой IP</b></td>
      <td>Везде</td>
      <td>Показывает, из какой страны вас видят сайты. Самая быстрая проверка,
        что VPN действительно работает.</td></tr>

    <tr><td><b>Обновление</b></td>
      <td>Android: «Ещё»<br>Mac: «Ещё»<br>Windows: «Проверка»</td>
      <td>Смотрит раз в сутки само и показывает карточку «Вышла версия…».
        Кнопка — на случай, если ждать не хочется.</td></tr>

    <tr><td><b>Диагностика</b></td>
      <td>Android и Mac: «Ещё»</td>
      <td>«Скопировать отчёт» — справка о том, как сейчас поднят туннель.
        Пригодится, если что-то не работает.</td></tr>
  </table>

  <h2 style="margin-top:5.5mm">А что настраивать не нужно</h2>
  <table>
    <tr><td style="width:48mm"><b>Маршруты</b></td>
      <td>Заблокированные сервисы всегда через VPN, российская зона — всегда
        напрямую. Режимов и правил нет.</td></tr>
    <tr><td><b>Исключения приложений</b><br><span style="color:#5B6C78">только
        Android</span></td>
      <td>МАХ, Сбербанк, ВТБ, Госуслуги, Ozon, Wildberries и ещё два десятка
        российских программ идут мимо туннеля целиком — для них VPN как будто
        выключен.</td></tr>
    <tr><td><b>DNS и IPv6</b></td>
      <td>Настраиваются сами и возвращаются как было при выключении.</td></tr>
  </table>

  <div class="note"><b>Значок включённого VPN.</b> На Android — ключик в строке
    состояния рядом с Wi-Fi и 4G, на Mac — щит в строке меню. Нет значка —
    защита выключена, как бы ни выглядело окно.</div>"""
    return page(9, "Обзор возможностей", body)


# ──────────────────── стр. 10 — если что-то не работает ────────────────────

def troubleshooting():
    body = """
  <h1>Если что-то не работает</h1>
  <p class="sub">Сначала — самое частое. Почти всё лечится за минуту.</p>

  <table>
    <tr><th style="width:62mm">Что вы видите</th><th>Что делать</th></tr>

    <tr><td><b>МАХ, банк или госуслуги пишут «отключите VPN»</b></td>
      <td>На Android этого быть не должно — эти программы идут мимо туннеля.
        Проверьте на главном экране карточку <b>«Мимо VPN целиком»</b>: программа
        должна быть в списке. Нет — обновите приложение.
        На Mac и Windows исключений по программам нет, но российские адреса
        и так идут напрямую; если ругается — пришлите название администратору.</td></tr>

    <tr><td><b>Видео крутится, сообщения при этом уходят</b></td>
      <td>Классика: сеть не пропускает крупные пакеты. Убедитесь, что размер
        пакета стоит на <b>«Авто»</b>, и переподключитесь — приложение подберёт
        размер само.</td></tr>

    <tr><td><b>Кнопка нажимается, но подключения нет</b></td>
      <td>Mac: проверьте, установлена ли служба — вверху окна не должно быть
        красной полосы. Windows: запущено ли от имени администратора.
        Везде: загружен ли ключ.</td></tr>

    <tr><td><b>«Проверить мой IP» показывает российский адрес</b></td>
      <td>Туннель не поднялся. Выключите и включите заново. Не помогло —
        скопируйте отчёт диагностики и пришлите.</td></tr>

    <tr><td><b>Совсем нет интернета при включённом VPN</b></td>
      <td>Выключите VPN — интернет вернётся сразу. Значит, до сервера не
        достучаться из этой сети: попробуйте другую сеть или
        <b>запасной вход</b>, если он вам выдан.</td></tr>

    <tr><td><b>В командировке в России мобильный интернет почти не работает</b></td>
      <td>Это не приложение. Оператор в такие дни пропускает только «белый
        список» сайтов, и туннель просто не устанавливается — обойти это
        со стороны телефона нельзя. Работают Wi-Fi и роуминговая eSIM.</td></tr>

    <tr><td><b>Mac: «приложение повреждено» или значок гаснет</b></td>
      <td>Снимите карантин — команда на странице 4.</td></tr>

    <tr><td><b>Windows: «не удалось запустить службу туннеля»</b></td>
      <td>Запущено не от администратора, либо рядом с
        <span class="mono">QPVPN.exe</span> нет
        <span class="mono">qpvpn-tunnel.exe</span> и
        <span class="mono">wintun.dll</span>.</td></tr>

    <tr><td><b>Скорость упала</b></td>
      <td>Нажмите «Замерить скорость» и пришлите четыре числа. Если и без VPN
        медленно — дело в сети, а не в приложении.</td></tr>
  </table>

  <div class="note" style="margin-top:6mm"><b>Что присылать, если ничего
    не помогло.</b> Четыре числа из замера скорости, отчёт диагностики
    («Ещё» &#8594; «Скопировать отчёт»), название сети (домашний Wi-Fi, мобильный
    интернет, кафе) и что именно не открывается. По одному слову «не работает»
    починить нельзя.</div>

  <div class="warn"><b>Чего делать не нужно.</b> Не пересылайте ссылку
    <span class="mono">vpn://…</span> и файл <span class="mono">.conf</span>
    в переписке «чтобы посмотрели» — в них приватный ключ. Для разбора
    достаточно отчёта диагностики: ключей в нём нет.</div>"""
    return page(10, "Если что-то не работает", body)


# ─────────────────────────────── сборка ───────────────────────────────

def build():
    pages = [cover(), android_install(), android_config(), mac_install(),
             mac_config(), win_install(), win_config(), iphone(),
             features(), troubleshooting()]
    html = f"""<!DOCTYPE html>
<html lang="ru"><head><meta charset="utf-8">
<title>QP VPN — инструкция</title>
<style>{CSS}</style>
</head><body>{''.join(pages)}</body></html>"""
    out = ROOT / "docs/instrukciya/index.html"
    out.write_text(html, encoding="utf-8")
    print("написано:", out, len(html), "байт")


if __name__ == "__main__":
    build()
